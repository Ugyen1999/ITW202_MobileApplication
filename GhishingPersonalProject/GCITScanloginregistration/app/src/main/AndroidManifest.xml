<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gcit.edu.gcit_scan_login_registration">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="GCIT-GateScan"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GCITScanloginregistration">
        <activity android:name=".Security"></activity>
        <activity android:name=".ForgotPassword" />
        <activity android:name=".Home" />
        <activity android:name=".userRegistration" />
        <activity android:name=".gateKeeperRegistration" />
        <activity android:name=".userLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>